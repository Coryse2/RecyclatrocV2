{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Recyclatroc - Inscription
{% endblock %}

{% block body %}
	<main class="global-container ">
<div class="card-header">
		<h1 class="breadcrumb">Inscription</h1>
		</div>
		<div class="signin">
			{{ form_start(form) }}
			{{ form_row(form.username) }}{{ form_row(form.email) }}
			{{ form_row(form.city, {'id':'city', 'attr':{'class':'form-control'} }) }}
			{#<input type="search" id="city" class="form-control" placeholder="Entrez votre ville ou votre code postal" />#}

			{{ form_row(form.avatar) }}
			{{ form_row(form.presentation) }}
			{{ form_row(form.phone) }}
			{{ form_rest(form) }}

			<div
				class="button-container">
				{#<button class="btn btn-outline-primary">{{ button_label|default('Sauvegarder') }}</button>
									</main> #}
				{{ include('user/_form.html.twig') }}
			</div>
<input type="button"  class="btn btn-outline-dark" value="Page précédente" onclick="history.go(-1)">
</form>
		</div>
		<!-- load JS -->
		<script src="https://cdn.jsdelivr.net/npm/places.js@1.17.1"></script>
		<script src="{{ asset('js/algo.js') }}"></script>
	</main>


{% endblock %}
